#Music Library Manager library/tool is used when generating a music library
#It basese the structure as: /BaseDir/Artist/Album
#Its meant to be used as a tool for the Music Manager Library/tool


import os


class LibraryManager(object):

    def __init__():
        """Inital class call"""


    def ifExists(fileName):
        """Checks to see if the file that is passed through"""


    def ifFolderExists(songItem):
        """Checks to see if the folder structure exsists for a new folder"""

    def createFolders(songItem):
        """Creates the folder structure for the song being added /Artist/Album/"""

    def moveFile(source,destination):
        """Moves the new file to its new collection"""
